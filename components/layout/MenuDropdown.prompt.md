Dropdown for the title-bar menus and every right-click menu — one global renderer styles them all.

```jsx
<MenuDropdown items={["Create XeBuild Image","Program Timing File","-","Settings"]} onSelect={run} />
```
