Dropdown for a fixed list (Kernel Version, Console Type). Highlighted rows use `--jr-accent-dim`, never a bright accent fill.

```jsx
<Select label="Console Type" value={type} options={["Falcon","Jasper","Corona","Trinity"]} onChange={setType} />
```
