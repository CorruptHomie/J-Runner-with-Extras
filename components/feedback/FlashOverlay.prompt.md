Use ONLY for an irreversible NAND write. It is the product's single moment of drama - everything else is instant and flat.

```jsx
<div style={{position:"relative"}}>{screen}<FlashOverlay value={42} /></div>
```

The parent must be `position: relative`. Escape is the escape hatch in the real app; keep a way out.
