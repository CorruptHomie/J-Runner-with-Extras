Shows which flasher J-Runner can see. Use the real device photographs in `assets/device-*.png` - never an icon or an illustration stand-in.

```jsx
<DeviceCard image="assets/device-picoflasher.png" name="PicoFlasher" detail="RP2040 - COM4" />
<DeviceCard />
```
